# ROM

!syntax description /ROM/ROMSetupAction

The `[ROM]` block wires a DEIM reduced-order-model training pipeline from a single block. See [ROMSetupAction.md] for the parameters, the generated object graph, and a worked example, and [modules/stochastic_tools/examples/deim_rom.md] for the method.

!syntax list /ROM objects=False actions=True subsystems=False
